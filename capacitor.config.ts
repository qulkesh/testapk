import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.docfinder',
  appName: 'DocFinder',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
